# -*- coding: utf-8 -*-
# ============================================================
# requirements.txt – chatbot-TMDT (SOPE)
# Cập nhật: 2026-07-12 | Task F01
# Chỉ giữ package đang dùng thực tế trong main.py và recommendation.py
# ============================================================

# --- Web framework & server ---
fastapi==0.136.3
starlette==1.1.0
uvicorn==0.48.0
websockets==15.0.1

# --- Pydantic / validation ---
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0

# --- HTTP clients (dùng trong main.py: httpx async, requests sync) ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
requests==2.32.5
urllib3==2.5.0
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10

# --- Gemini / Google AI SDK ---
google-genai==2.13.0
google-auth==2.56.2

# --- Data science / ML (dùng trong recommendation.py) ---
numpy==2.3.3
pandas==2.3.2
scipy==1.17.1
scikit-learn==1.9.0  # TF-IDF và cosine_similarity trong recommendation.py

# --- Async / concurrency ---
anyio==4.11.0
sniffio==1.3.1

# --- Env / config ---
python-dotenv==1.2.2

# --- Utilities ---
click==8.4.1
colorama==0.4.6
tqdm==4.67.1
tenacity==9.1.2
packaging==24.1
six==1.17.0
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2024.2
setuptools==75.1.0

# --- Template engine (Starlette dùng nội bộ) ---
Jinja2==3.1.6
MarkupSafe==3.0.3

# --- Misc ---
distro==1.9.0

# ============================================================
# Các package ĐÃ XÓA (F01):
#   - pymysql          → E01: thay bằng REST API
#   - torch, torchvision → chưa dùng, quá nặng
#   - openai           → dùng Gemini, không cần OpenAI SDK
#   - SDK Gemini Python legacy → đã hết hỗ trợ, thay bằng google-genai
#   - Django, asgiref, sqlparse → framework khác
#   - matplotlib + deps (contourpy, cycler, fonttools, kiwisolver, pyparsing) → vẽ đồ thị, không dùng
#   - opencv-python    → xử lý ảnh, không dùng
#   - pygame           → game library
#   - customtkinter, CTkMessagebox, darkdetect → GUI desktop
#   - pyinstaller, pyinstaller-hooks-contrib, altgraph, pefile, pywin32-ctypes → đóng gói exe
#   - more_function    → không rõ nguồn, không thấy import
# ============================================================
